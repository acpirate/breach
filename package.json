{
  "name": "breach-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx scripts/smoke.ts",
    "batch": "tsx scripts/batch.ts",
    "hpladder": "tsx scripts/hpladder.ts",
    "logs:dump": "node scripts/logs.cjs dump",
    "logs:wipe": "node scripts/logs.cjs wipe"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vite": "^6.0.0"
  }
}
